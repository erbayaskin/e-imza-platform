package io.github.erbayaskin.eimza.core.model;

/** Defines whether a signing session creates a first, co-signature, or counter-signature. */
public enum MultiSignatureType {
    SINGLE,
    PARALLEL,
    SERIAL
}
